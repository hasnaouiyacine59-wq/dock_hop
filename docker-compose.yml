services:
  vpn-browser:
    image: quay.io/mylastres0rt05_redhat/norvya:latest
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun
    environment:
      - NORDVPN_TOKEN=${NORDVPN_TOKEN}
    restart: unless-stopped
    deploy:
      replicas: 2

